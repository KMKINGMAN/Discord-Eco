import fs from "fs";
import efs from "fs-extra";
import dotenv from "dotenv";
import * as mongo from "mongoose";
import chalk from "chalk";
import figlet from "figlet";
import ms from "ms";
import * as btn_pages from "djs-button-pages";
export {
    fs,
    efs,
    dotenv,
    mongo,
    chalk,
    figlet,
    ms,
    btn_pages
}